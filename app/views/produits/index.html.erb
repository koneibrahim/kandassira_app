 <p id="notice"><%= notice %></p>
 <div class="container-fluid">
    <div class="row">
      <div class="col-xs-2 col-sm-2">
        </div>
      <div class="col-xs-8 col-sm-8">
        <h1>Produits</h1>
        <div class="table-responsive">
          <table class="table table-bordered table-condensed table-striped">
          <thead>
              <tr class="info">
                <th>N°</th>
                <th>Nom produit</th>
                <th>Type produit</th>
                <th>Prix</th>
                <th>Qte produit</th>
                <th colspan="3"></th>
              </tr>
          </thead>

        <tbody>
            <%
            @i=1;
            @produits.each do |produit|
              %>
              <tr>
                  <td><%= @i %></td>
                  <td class=""><%= produit.nom_pro %></td>
                  <td><%= produit.type_pro %></td>
                  <td><%= produit.prix %></td>
                  <td><%= produit.qte_pro %></td>
                  <td><%= link_to 'Show', produit %></td>
                  <td><%= link_to 'Edit', edit_produit_path(produit) %></td>
                  <td><%= link_to 'Destroy', produit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
              <%
              @i+=1
            end
            %>
        </tbody>
        <br>
                <a href="/produits/new" class="btn btn-primary btn-block">
                  <span class="glyphicon glyphicon-plus"></span> Add new produit</a></p>
                  </table>
                  </div>
                  <div class="col-xs-2 col-sm-2">
                    </div>
            </div>
          </div>
